# Level 1+: run API locally in Docker (optional).
services:
  api:
    build: ./backend
    ports:
      - "3847:3847"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app
    command: npm run dev
