# Legacy — not used

This Fastify scaffold was from an earlier “own LLM API” idea.

**Product decision:** analysis runs only in the user’s free chats (ChatGPT Custom GPT / Gemini web). See `docs/PRODUCT.md`.

Do not extend this folder for fact-checking. It should be removed during Level 1 cleanup.
